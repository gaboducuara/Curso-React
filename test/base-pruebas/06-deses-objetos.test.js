import { usContext } from "../../src/base-pruebas/06-deses-obj"

describe('Pruebas en desestructuracion de objetos', () => {

    test('userContext debe retornar Parametros', () => {

        const user = (usContext)
        console.log(user)
    })
})